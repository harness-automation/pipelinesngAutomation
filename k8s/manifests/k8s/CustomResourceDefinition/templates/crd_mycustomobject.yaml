apiVersion: example.com/v1
kind: {{.Values.name}}-crd-sample
metadata:
  name: {{.Values.name}}-crd-object
  annotations:
    "harness.io/managed-workload": "true"
spec:
  replicas: 1
  environment: dev
  version: release-1.0.0
  language: golang
